/*判断能否构成三角形*/
#include "stdio.h"
int main()
{
float a,b,c;
printf("请输入三个大于0的实数:(以空格分开)\n");
 scanf("%lf%lf%lf",&a,&b,&c);
if((a+b)>c && (a+c)>b && (b+c)>c) 
printf("能够成三角形\n");
 else 
	 printf("不能构成三角形\n");
 return 0;
}
